# Same Postgres instance/database as "Greenly - Salary", but isolated in its
# own schema (namespace) via ?schema=so_check — keeps migration history separate.
DATABASE_URL="postgresql://user:password@localhost:5432/greenly?schema=so_check"
